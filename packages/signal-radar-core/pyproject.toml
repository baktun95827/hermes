[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "signal-radar-core"
version = "0.1.0"
description = "Runtime-neutral contracts and memory primitives for Signal Radar."
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]

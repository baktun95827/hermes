interface:
  display_name: "Signal Radar"
  short_description: "Collect remote social signals, cluster new posts, and keep running memory."
  default_prompt: "Use $signal-radar to collect recent posts from my configured X accounts, produce a Chinese topic brief, and persist the MEMORY_UPDATE back into the skill state."

policy:
  allow_implicit_invocation: true
